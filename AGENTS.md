# AGENTS

## 作用范围

本文件对整个 `solution-hub` 仓库生效；如果子目录下存在更近的 `AGENTS.md`，则以更近的文件为准。

## 仓库定位

- `solution-hub` 是一个专题方案聚合仓，不是单一业务系统。
- 它的目标是沉淀“可运行、可讲解、可迁移”的解决方案样例，而不是承载线上业务全量逻辑。
- 当前根 `pom.xml` 聚合以下专题：
  - `solution-auth`
  - `solution-i18n`
  - `solution-i18n-brief`
  - `solution-netty`
  - `solution-rtsp`
- 适合在这里做的事情：
  - 技术预研
  - 通用能力封装
  - 最小可运行示例
  - 方案对比与演进
- 如果某项能力已经明确进入正式业务场景，后续通常应考虑回灌到 `knowledge-hub`，而不是长期只停留在样例仓。

## 专题模块地图

### `solution-auth`

- 定位：认证与基础服务方案集。
- 当前子模块包括：
  - `form-dict-sample`
  - `jwt-login-sample`
- 适用场景：
  - 登录鉴权基线验证
  - 表单字典、模板化录入类能力的样例整理
- 修改建议：
  - 保持每个样例主题单一、目的明确。
  - 不要为了补业务功能把多个认证主题强行耦合到一起。

### `solution-i18n`

- 定位：较完整的国际化能力方案。
- 当前子模块包括：
  - `i18n-core`
  - `i18n-core-sample`
- 已有特点：
  - 注解驱动
  - `ResponseBodyAdvice` 自动翻译
  - 枚举、List、Map、嵌套对象处理
  - 支持参数化文案
- 适用场景：
  - Spring Boot 项目做相对完整的 DTO 输出国际化
  - 需要作为公共能力抽象的国际化基线

### `solution-i18n-brief`

- 定位：轻量化国际化方案。
- 当前子模块包括：
  - `i18n-core-brief`
  - `i18n-core-brief-sample`
- 已有特点：
  - 默认中文输出
  - 仅在英文环境自动翻译
  - 支持 starter 自动装配
- 适用场景：
  - 主要语言为中文，只需对英文场景做补充翻译
  - 追求接入轻量、认知成本低

### `solution-netty`

- 定位：面向 TCP 设备接入的 Netty 方案样例。
- 当前子模块包括：
  - `netty-core`
  - `netty-sample`
- 已有特点：
  - TCP 服务端 / 客户端
  - 会话管理
  - 心跳
  - 断线重连
  - JSON + 换行符分帧协议示例
- 适用场景：
  - IoT 接入前的链路验证
  - 设备协议接入原型
  - 基于 Netty 的最小可运行服务端样例

### `solution-rtsp`

- 定位：RTSP 标准化接入方案。
- 当前子模块包括：
  - `rtsp-core`
  - `rtsp-ffmpeg-bridge`
  - `rtsp-netty-transport`
  - `rtsp-sample`
- 已有特点：
  - RTSP 会话协商
  - `mock / netty / ffmpeg` 多 provider 路线
  - 适合从协议理解到生产落地逐步演进
- 适用场景：
  - 摄像头、NVR、边缘网关的 RTSP 拉流接入基线
  - 控制面握手验证
  - 后续转码、截图、录像链路对接

## 使用这个仓库的正确方式

### 做技术选型或方案预研

1. 先选最接近目标场景的专题模块。
2. 读该专题 `README.md` 和样例说明。
3. 在该专题内部完成验证。
4. 验证稳定后，再考虑提炼到正式业务仓。

### 为主仓抽公共能力

1. 可以先在这里做原型。
2. 尽量把能力边界、接入方式、示例调用整理清楚。
3. 再决定是否沉淀到 `knowledge-hub` 的共享模块。

### 做样例增强

- 优先增强专题完整性，而不是横向塞入不相关能力。
- 样例首先要“好理解”，其次才是“功能多”。

## 模块边界约定

- 各 `solution-*` 尽量保持专题独立，不要制造跨专题强耦合。
- 如果多个专题确实需要共享能力，优先评估是否应抽成独立公共模块，而不是直接相互依赖源码实现。
- 示例模块与核心模块要尽量分离：
  - `*-core` 负责核心能力
  - `*-sample` 负责演示接入与验证
- 不要把大量一次性调试代码直接留在 `core` 里。

## 构建与验证建议

- 根工程可作为统一 Maven 聚合入口。
- 日常修改时优先定向构建目标专题，例如：
  - `mvn -pl solution-i18n -am test`
  - `mvn -pl solution-netty -am test`
- 如果某个专题自带 sample，优先通过 sample 验证接入行为，而不是只跑单元测试。
- 修改核心能力后，尽量同步验证其 sample 模块，避免“core 改了但示例失效”。

## 建议阅读顺序

1. 根 `pom.xml`
2. 本文件
3. 目标 `solution-*` 的 `README.md`
4. 目标专题下 `core` 与 `sample` 的目录结构
5. 如有测试手册、操作手册、架构说明，再继续下钻

## 适合回灌到正式仓的内容

- 已经稳定的 starter / 核心抽象
- 明确可复用的工具类、接口边界、协议处理组件
- 在样例里反复验证过的接入模式

## 不适合长期停留在这里的内容

- 与具体业务强绑定的流程代码
- 某个线上项目独有的配置堆叠
- 为了临时联调加入的大量环境耦合逻辑

## 文档维护约定

- 新增专题模块时，应同步更新根 `pom.xml`、本文件和根 `README.md`。
- 某个专题的定位、模块划分、验证方式变化较大时，应先更新该专题自己的 `README.md`，再回写本文件。
- 如果某个专题已经形成稳定的模块内协作规范，建议在该专题目录继续补充更近一层的 `AGENTS.md`。
